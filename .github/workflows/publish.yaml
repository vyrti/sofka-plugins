name: Publish changed plugins

on:
  push:
    branches: [main]
    paths:
      - "plugins/**"

permissions:
  contents: read

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      plugins: ${{ steps.changes.outputs.plugins }}
      matrix: ${{ steps.changes.outputs.matrix }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - id: changes
        env:
          BASE: ${{ github.event.before }}
          HEAD: ${{ github.sha }}
        run: |
          changes="$(python3 scripts/catalog.py changes --base "$BASE" --head "$HEAD" --mode publish)"
          echo "plugins=$(jq -c .plugins <<<"$changes")" >> "$GITHUB_OUTPUT"
          echo "matrix=$(jq -c .matrix <<<"$changes")" >> "$GITHUB_OUTPUT"
      - name: Reject replacement or modification of a published version
        env:
          BASE: ${{ github.event.before }}
          HEAD: ${{ github.sha }}
        run: python3 scripts/catalog.py assert-unpublished --base "$BASE" --head "$HEAD"

  validate:
    needs: changes
    if: needs.changes.outputs.plugins != '[]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # stable
        with:
          components: clippy, rustfmt
      - run: python3 scripts/catalog.py validate
      - run: python3 scripts/test_catalog.py
      - run: cargo fmt --all -- --check
      - run: cargo clippy --locked --workspace --all-targets -- -D warnings
      - run: cargo test --locked --workspace

  build:
    needs: [changes, validate]
    if: needs.changes.outputs.plugins != '[]'
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.changes.outputs.matrix) }}
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # stable
        with:
          targets: ${{ matrix.target }}
      - name: Build and test adapter
        env:
          PLUGIN: ${{ matrix.plugin }}
          TARGET: ${{ matrix.target }}
        run: |
          cargo build --release --locked --package "sofka-plugin-$PLUGIN" --target "$TARGET"
          "target/$TARGET/release/sofka-plugin-$PLUGIN" < "plugins/$PLUGIN/fixtures/request.json" > report.json
          python3 -c 'import json,sys; assert json.load(open(sys.argv[1])) == json.load(open(sys.argv[2]))' report.json "plugins/$PLUGIN/fixtures/report.json"
          version="$(python3 scripts/catalog.py version --plugin "$PLUGIN")"
          python3 scripts/catalog.py package \
            --plugin "$PLUGIN" \
            --target "$TARGET" \
            --binary "target/$TARGET/release/sofka-plugin-$PLUGIN" \
            --output "$PLUGIN-$version-$TARGET.tar.gz"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ matrix.plugin }}-${{ matrix.target }}
          path: ${{ matrix.plugin }}-*.tar.gz

  publish:
    needs: [changes, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write
      attestations: write
    env:
      GH_TOKEN: ${{ secrets.CATALOG_GITHUB_TOKEN }}
      PLUGINS_JSON: ${{ needs.changes.outputs.plugins }}
      SOURCE_COMMIT: ${{ github.sha }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          token: ${{ secrets.CATALOG_GITHUB_TOKEN }}
          persist-credentials: true
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: dist
          merge-multiple: true
      - name: Attest package bytes
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: dist/*.tar.gz
      - name: Publish immutable release assets
        run: |
          for plugin in $(jq -r '.[]' <<<"$PLUGINS_JSON"); do
            version="$(python3 scripts/catalog.py version --plugin "$plugin")"
            tag="$plugin-v$version"
            if ! gh release view "$tag" >/dev/null 2>&1; then
              gh release create "$tag" dist/$plugin-$version-*.tar.gz \
                --target "$SOURCE_COMMIT" \
                --title "$plugin $version" \
                --notes "Reviewed sofka plugin package $plugin $version."
            fi
            mkdir -p "published/$plugin"
            gh release download "$tag" --pattern "$plugin-$version-*.tar.gz" --dir "published/$plugin"
            for asset in dist/$plugin-$version-*.tar.gz; do
              cmp "$asset" "published/$plugin/$(basename "$asset")"
            done
          done
      - name: Generate catalog entry from published bytes
        run: |
          mapfile -t plugins < <(jq -r '.[]' <<<"$PLUGINS_JSON")
          python3 scripts/catalog.py update-index --commit "$SOURCE_COMMIT" --assets dist "${plugins[@]}"
          python3 scripts/catalog.py validate
      - name: Open reviewed catalog update
        run: |
          branch="catalog/publish-${SOURCE_COMMIT:0:12}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git ls-remote --exit-code --heads origin "$branch" >/dev/null; then
            gh pr view "$branch" >/dev/null 2>&1 || gh pr create \
              --base main \
              --head "$branch" \
              --title "chore: publish plugin catalog entries" \
              --body "Publishes the reviewed plugin assets built from $SOURCE_COMMIT. Implements https://github.com/nklmilojevic/sofka/issues/502 as agreed in https://github.com/nklmilojevic/sofka/discussions/501."
            exit 0
          fi
          git switch -c "$branch"
          git add index.json
          git commit -m "chore: publish plugin catalog entries"
          git push origin HEAD
          gh pr create \
            --base main \
            --head "$branch" \
            --title "chore: publish plugin catalog entries" \
            --body "Publishes the reviewed plugin assets built from $SOURCE_COMMIT. The release assets existed and matched these recorded SHA-256 digests before this pull request was opened. Implements https://github.com/nklmilojevic/sofka/issues/502 as agreed in https://github.com/nklmilojevic/sofka/discussions/501."
